import React from "react";
import {Routes, Route} from "react-router-dom"
import "./App.css"

const App = ( ) => {
return (
  <div>
    <Routes>
        <Route>
        </Route>
    </Routes>
  </div>
)
}

export default App
